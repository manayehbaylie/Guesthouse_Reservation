# PostgreSQL connection string (update password for your local machine)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/guesthouse_reservation?schema=public"

# JWT secret for authentication tokens (change in production)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Server port (default: 5000)
PORT=5000
